name: Fedora
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "Fedora"
        run: "./containerize.sh --runtime=docker --distro=fedora --tag=latest"

