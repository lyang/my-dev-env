---
- name: "Install dconf-cli"
  become: true
  ansible.builtin.apt:
    state: latest
    autoremove: true
    install_recommends: false
    name: dconf-cli
  when: ansible_os_family|lower == "debian"

